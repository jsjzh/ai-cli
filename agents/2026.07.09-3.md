## 需求

新建一个 souche 的 controller，然后再新建一个功能，叫 sync

## 详细

1. 使用 nvm 切换到 node@12
2. 将 registry 设置成 http://registry.npm.souche-inc.com/
3. 检测全局是否有 snpm，如果没有的话，提示安装
4. 获取当前目录下的所有 dep devdep
5. 执行 snpm sync dep-1 dep-2 dep-3 dep-xxx，每次最多 sync 10 个 dep，分批执行，直到执行完
6. 切换回原本的 registry 和 node 版本
