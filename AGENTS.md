# 工作流规则

## 每当完成一个功能或修复后

1. **更新 CHANGELOG.md** — 在对应版本的 `### 功能补全`、`### BUG 修复`、`### 架构优化` 等分区下添加条目，描述本次改动
2. **更新 README.md** — 如果新增/删除了命令或改变了行为，同步更新 README 中的命令表和说明
3. **提交代码** — 完成上述更新后，询问用户是否要 git commit，如果同意则执行（先 `git add` 再 `git commit`）
4. **更新版本** — 如果用户同意提交代码，在提交代码之前要更新一下 package.json 中的 version
